This bundled `humanizer-xhs` is a local Xiaohongshu-specific adaptation inspired by humanizer-style AI-writing cleanup workflows.

It is not a verbatim copy of an external repository. If a fuller external humanizer skill is installed in the current agent, the agent may use it, but this bundled file remains the fallback copy gate.
